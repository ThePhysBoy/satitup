# ระบบ Quick Links พร้อมคู่มือการใช้งาน

## ภาพรวม
ระบบ Quick Links ได้รับการปรับปรุงให้มีการแสดงผลที่สวยงามและใช้งานง่ายมากขึ้น พร้อมเพิ่มฟีเจอร์ปุ่มคู่มือการใช้งานสำหรับแต่ละระบบ

## คุณสมบัติใหม่

### 1. การแสดงผลแบบ Grid Layout
- ใช้ CSS Grid สำหรับการจัดเรียงไอคอนแบบอัตโนมัติ
- รองรับการแสดงผลที่ยืดหยุ่นตามขนาดหน้าจอ
- ไม่มีปัญหาไอคอนเศษในแถวสุดท้าย

### 2. ปุ่มคู่มือการใช้งาน
- ปุ่มไอคอนหนังสือ 📚 ที่มุมขวาบนของแต่ละการ์ด
- แสดงเมื่อ hover เมาส์เหนือการ์ด
- เมื่อคลิกจะเปิด Modal แสดงตัวเลือกคู่มือ

### 3. รูปแบบคู่มือที่รองรับ
- **PDF**: ดาวน์โหลดคู่มือในรูปแบบ PDF
- **วิดีโอ**: ลิงก์ไปยังวิดีโอสอนใช้งาน (YouTube)
- **เอกสารออนไลน์**: ลิงก์ไปยัง Google Docs หรือเว็บคู่มือ

## โครงสร้างไฟล์

```
satitup/
├── quick_links.php          # ไฟล์หลักสำหรับแสดง Quick Links
├── css/
│   └── style-school.css     # CSS สำหรับการแสดงผล
├── manuals/
│   ├── manual_links.json    # ข้อมูลลิงก์คู่มือทั้งหมด
│   └── [PDF files]          # ไฟล์ PDF คู่มือ (ถ้ามี)
```

## การตั้งค่า

### 1. ไฟล์ manual_links.json
```json
{
    "system-key": {
        "title": "ชื่อคู่มือ",
        "pdf_url": "/manuals/file.pdf",
        "video_url": "https://youtube.com/...",
        "online_doc": "https://docs.google.com/..."
    }
}
```

### 2. การเพิ่มระบบใหม่
1. เพิ่มการ์ดใหม่ใน `quick_links.php`
2. เพิ่มข้อมูลคู่มือใน `manual_links.json`
3. อัปโหลดไฟล์ PDF (ถ้ามี) ไปที่โฟลเดอร์ `manuals/`

## การแสดงผล Responsive

### Desktop (>1200px)
- แสดง 5-6 ไอคอนต่อแถว
- ขนาดการ์ดมาตรฐาน

### Tablet (768px - 1200px)
- แสดง 3-4 ไอคอนต่อแถว
- ขนาดการ์ดปานกลาง

### Mobile (576px - 768px)
- แสดง 2-3 ไอคอนต่อแถว
- ขนาดการ์ดเล็กลง

### Mobile Small (<576px)
- แสดง 2 ไอคอนต่อแถว
- ขนาดการ์ดเล็กที่สุด

### Mobile Extra Small (<400px)
- แสดง 1 ไอคอนต่อแถว
- การ์ดกว้างเต็มหน้าจอ

## การปรับแต่ง

### เปลี่ยนสีปุ่มคู่มือ
แก้ไขใน `style-school.css`:
```css
.manual-link {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### เปลี่ยนขนาดการ์ด
แก้ไขใน `style-school.css`:
```css
.quick-links-grid {
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
}
```

### เพิ่มประเภทคู่มือใหม่
แก้ไข JavaScript ใน `quick_links.php` เพื่อเพิ่มประเภทใหม่ในส่วนการสร้าง modal content

## การบำรุงรักษา

1. **อัปเดตคู่มือ**: แก้ไขไฟล์ `manual_links.json`
2. **เพิ่มระบบใหม่**: เพิ่มทั้งใน PHP และ JSON
3. **แก้ไขการแสดงผล**: ปรับแต่ง CSS ใน `style-school.css`

## ข้อควรระวัง

1. ตรวจสอบให้แน่ใจว่า key ใน `data-manual` attribute ตรงกับ key ใน JSON
2. URL ของคู่มือควรใช้ path แบบ relative สำหรับไฟล์ภายใน
3. ทดสอบการแสดงผลในทุกขนาดหน้าจอ

## การแก้ปัญหา

### ปุ่มคู่มือไม่แสดง
- ตรวจสอบว่ามี FontAwesome loaded
- ตรวจสอบ CSS class `.manual-link`

### Modal ไม่เปิด
- ตรวจสอบว่ามี Bootstrap JS loaded
- ตรวจสอบ console log สำหรับ error

### คู่มือไม่โหลด
- ตรวจสอบ path ของ `manual_links.json`
- ตรวจสอบ JSON syntax
- ดู Network tab ใน Developer Tools

## อนาคต

สิ่งที่สามารถพัฒนาเพิ่มเติม:
1. ระบบค้นหาในคู่มือ
2. การติดตาม analytics การใช้งานคู่มือ
3. ระบบแนะนำคู่มือตามการใช้งาน
4. คู่มือแบบ interactive
5. การแปลคู่มือหลายภาษา
